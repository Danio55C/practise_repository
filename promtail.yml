
server:
  http_listen_port: 9080
  grpc_listen_port: 0
positions:
  filename: /tmp/positions.yaml

clients:
  - url: http://elasticsearch:9200/_bulk
  
 

scrape_configs:
- job_name: system
  static_configs:
  - targets:
      - "localhost"
    labels:
      job: logs
      __path__: "/var/logs/file_logs.txt"


        
        
